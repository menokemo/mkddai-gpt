# Docs Sync

Use this when docs are updated locally:

```bash
chmod +x sync_docs.sh
./sync_docs.sh
```

Optional custom commit message:

```bash
./sync_docs.sh "docs: update current AI Factory status"
```
